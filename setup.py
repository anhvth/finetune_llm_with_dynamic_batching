# setup.py
from setuptools import setup, find_packages

setup(
    name="efficient_finetuning_with_dynamic_batching",
    version="0.0.1",
    author="Your Name",
    description="A project for efficient finetuning with dynamic batching",
    packages=find_packages(),
    # install_requires=["numpy", "t, "scikit-learn",],
)
